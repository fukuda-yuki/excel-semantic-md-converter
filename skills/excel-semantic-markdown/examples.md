# Examples

Run the launcher with an input workbook and an output directory:

```powershell
.\run_excel_semantic_md.ps1 -InputPath "C:\work\sample.xlsx" -OutDir "C:\work\out"
```

The launcher validates paths and delegates conversion to the Python CLI.
