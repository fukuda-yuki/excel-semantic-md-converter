# phase1-ooxml-visual-metadata review note

## Review Scope

- Milestone: `phase1-ooxml-visual-metadata`
- Scope: raw OOXML visual metadata reader、`inspect` への `visuals` 追加、関連 phase docs、synthetic visual fixtures、OOXML visual tests。
- Out of scope: block への visual 紐付け、manifest writer、Excel COM rendering、Markdown writer、LLM integration。
- Source of Truth: `docs/phase1/spec.md`

## Changed Files

- `docs/phase1/spec.md`
- `docs/phase1/task.md`
- `docs/phase1/knowledge.md`
- `docs/review/phase1/phase1-ooxml-visual-metadata.md`
- `src/excel_semantic_md/cli/main.py`
- `src/excel_semantic_md/excel/__init__.py`
- `src/excel_semantic_md/excel/ooxml_visual_reader.py`
- `tests/test_ooxml_visual_reader.py`
- `tests/test_workbook_reader.py`
- `tests/fixtures/visuals/no-visuals.xlsx`
- `tests/fixtures/visuals/image-visual.xlsx`
- `tests/fixtures/visuals/chart-visual.xlsx`
- `tests/fixtures/visuals/shape-unknown.xlsx`
- `tests/fixtures/visuals/broken-drawing-rel.xlsx`
- `tests/fixtures/visuals/image-visual.xlsm`

## Subagents

- spec compliance and functional correctness reviewer: not started
  - Reason: current session policy allows `spawn_agent` only when the user explicitly asks for sub-agents or delegation.
- tests, edge cases, and regression risk reviewer: not started
  - Reason: current session policy allows `spawn_agent` only when the user explicitly asks for sub-agents or delegation.

## Raw Findings Summary

- Self-review only:
  - Accepted and fixed during implementation: OOXML relationship target resolution needed the source part path to normalize `../` targets correctly for sheet rels and drawing rels.
  - Accepted and fixed during implementation: chart fixture generated by openpyxl did not include cached category/value points, so the synthetic fixture was patched to assert `categories` / `values` output deterministically.
  - `python -m pytest tests/test_ooxml_visual_reader.py tests/test_workbook_reader.py tests/test_cli.py tests/test_block_detector.py tests/test_models.py` passed.
- Pending externalized review:
  - spec compliance / functional correctness review has not been run by a subagent.
  - tests / edge cases / regression review has not been run by a subagent.

## MainAgent Validity Judgment

- `inspect` now preserves workbook reading JSON and block JSON, while appending sheet-level `visuals` as required by the milestone.
- OOXML visual metadata is isolated in a dedicated raw OOXML reader and does not widen the shared block models.
- Unsupported visual payloads currently degrade to `unknown` visuals or sheet / visual warnings, which matches the milestone scope.
- Milestone review requirements are not fully satisfied yet because the required subagent review could not be started under the current session policy.

## Response Plan

- Keep the implementation, fixtures, and tests in place.
- If the user explicitly authorizes subagent/delegated review, run the two required reviewers and update this note with their findings before declaring the milestone complete.
- If the user does not want subagents, keep this note as a partial review record and leave the remaining gap explicit.

## Applied Fixes

- Added `read_visual_metadata()` and exported it from `excel.__init__`.
- Implemented raw OOXML drawing/image/shape/chart parsing, anchor extraction, unsupported visual degradation, and OOXML source metadata capture.
- Extended `inspect` to append `visuals` and merge visual-reader warnings into per-sheet warnings.
- Added sanitized static fixtures for image, chart, shape, unknown group shape, broken drawing relationship, no-visual workbook, and `.xlsm`.
- Updated phase docs to record the `visuals` JSON contract and the current `asset_candidate` scope.

## Residual Risks

- `absoluteAnchor` is preserved as metadata only; no A1 approximation is attempted in this milestone.
- Chart `series.name` currently falls back to the OOXML ref formula when the chart part does not cache display text.
- SmartArt / OLE / group shape are intentionally not fully interpreted and will remain `unknown` until a later milestone.
- The review workflow still lacks the required subagent pass, so regression / spec gaps may remain undiscovered.

## Pending Items

- Obtain explicit authorization for subagent review, then rerun and update this note.
